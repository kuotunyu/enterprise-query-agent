# Enterprise Query Agent：施工總計畫

更新：2026-09-14。**這是本專案唯一的施工入口。開發 session 按本文順序完成工作，不需要使用者在多份文件與兩個 task 之間搬運指令。** 使用者已於完成報告審查後批准公開發布至 `kuotunyu/enterprise-query-agent`；下方先前未授權敘述屬歷史階段，現以此批准及 docs/status.md 為準。此批准不包含雲端部署或追加研究。

## 一、要交付什麼

一個可在本機操作的中文電商資料查詢工作台：使用者提問，系統釐清營收等業務定義，查詢 MySQL，回傳可核對的答案、結果表、使用的口徑與資料依據。

最後交付：真實 Olist 資料上的可操作產品、三種查詢方法的一次比較、可重現的結果與失敗紀錄，以及可供求職展示的 GitHub 原始碼與示範。

範圍固定：一個電商領域、一種 MySQL 引擎、一個 OpenAI API 模型。v1 不加入其他產業、多模型排名、微調、多 Agent、第二資料庫或 GPU 部署。

## 二、哪些已完成

**M1–M3 本機工程已完成，不重新施工。** 已包含獨立資料庫、合成資料、業務查詢計畫、受控 SQL、數字與證據、中文 UI、mock 流程及工程測試。

工程基線 `2520a430a5775d1eb58191536e7739f09851da81`；文件收尾 `a958b7d`。實際驗證與限制見 [工程狀態](docs/status.md)。mock 的成功率不能寫成真實模型能力。

接下來只做以下四步。只有找到違反既定契約的具體 bug 才修原功能，不因開始新階段而重寫已完成部分。

## 三、依序施工

### 第一步：真實資料與比較工具準備

**現在可執行，由開發 session 自行處理。**

- 在前作 Olist checkout 的 data/raw 找九份 CSV，核對檔名、可讀性與 SHA-256。資料路徑可自行查找，不要求使用者重新提供。
- 保留前作 checkout、CSV、DB、容器與 volume。使用本案識別的獨立環境匯入；保留合成測試環境的可重建方式，真資料驗證不要被合成 bootstrap 覆蓋。
- 核對 row counts、關聯、資料期間、已知缺值及 manifest；確認工作台顯示真實資料身分。真實日期不能繼續顯示 synthetic-v1。
- 對真資料執行有限、事先選定的開發查詢，使用前作結果及獨立計算核對金額／分母；這是資料與工程驗證，不是模型成績。
- 準備 A 固定模板＋slot extraction、B 直接生成 SQL、C 業務計畫＋程式編譯三種方法與共用結果紀錄；目前 C 已有工程，A/B 依實際缺口補齊。
- 三方法共用資料、業務定義、模型版本、澄清規則、SQL 執行防護與額度。先以離線 mock／契約測試確認 runner，不呼叫付費 API。
- 依當日官方可用性，選一個支援目前 structured-output adapter 的模型，記錄確切 model ID、参数與費率。估算開發與正式研究各自費用，整理成一份方案給使用者一次決定。

**完成標準：** 真資料匯入與核對完成、三方法有可用比較入口、模型與分項費用方案已具體列出。缺 API key 或費用授權不阻止這一步可獨立完成的工作。

### 第二步：真實模型的開發驗證

**開始條件：** 使用者已同意具體模型與費用上限，所需憑證已在本機安全設定。

- 用至少 24 個公開開發問題驗證自然語言理解、澄清、查詢与答案。現有驗收題、26 題 mock corpus、前作十題都屬開發資料。
- 修正格式、介面或業務語意的實際錯誤；記錄每次 run 的模型、prompt、成本與失敗。只在開發資料上調整。
- 完成三方法端到端比較入口，確認所有錯答、拒絕、逾時與不支援任務都能被記錄。
- 保持同一模型；不因某次失敗自行換模型、增加付費額度或開新研究方向。

**完成標準：** 三方法能在同一真資料快照下執行並正確記錄結果；沒有未處理的 adapter／runner 故障。模型可以答錯，不能把「全部題目都答對」當作無限開發的條件。

### 第三步：凍結版本，完成一次正式評估

**開始條件：** 第二步完成，正式研究費用在已同意額度內。若一次已核准兩階段額度，不重複要求同一批准。

- 凍結 code、prompt、catalog、data、model、參數、費率、資源限制、評分與恢復規則，記錄在單一 run manifest。
- 安排不修改候選程式的評估工作，凍結後製作 40 個保留任務：20 可直接答、8 需澄清、6 缺資料／超出範圍、6 拒絕／限制。另為其中 12 題各準備兩個合成資料變體。
- gold 以獨立計算核對，不用候選 compiler 當唯一標準；gold 不進候選模型 prompt。作者與 AI 參與重疊如實揭露，不稱人類盲測。
- 依既定順序每方法每任務跑一次，保留所有 terminal outcome。變體與原題相依，不加總成更多獨立任務。
- 報告任務成功、口徑／時間／數值正確、澄清、無依據回答、成本、延遲與典型失敗。全體分母保留不支援任務，另列共通支援子集。

**完成標準：** 預定比較完整、結果可核對、所有失敗保留。C 沒有勝過 A/B 也能完成；不能為正增益持續調參重跑保留題。

### 第四步：展示、發布、結案

- 完成真實產品使用說明、兩分鐘操作示範、主要設計取捨與代表性失敗解說。
- 核對乾淨來源啟動、資料取得方式、必要測試與可公開結果；保留來源授權，不提交 raw CSV、憑證或私人路徑。
- 在目前正式帳號下準備 `enterprise-query-agent` 的發布內容；先確認名稱可用。取得發布授權後才建立 remote、push 與 release；不覆寫現存 repo。
- 更新 README 與工程狀態，明確標記 v1 完成、研究限制與支援範圍。

**完成標準：** 原始碼與已授權證據可公開檢查，示範可操作，研究宣稱符合實測。到此結案；其他模型、資料庫、產業或功能另案決策。

## 四、分工與回報

- **開發 session：** 自行處理查資料、設定、實作、啟動、測試、修復與文件更新；按上述順序持續施工。例行問題自行解決，不把命令逐條交給使用者執行。
- **戰情室：** 負責定位、範圍與必要決策；不與開發 session 同時修改應用程式，不每完成小步就要求再審一次。
- **使用者：** 只有既有指示要求的付費／發布決策，以及無法代辦的登入／憑證步驟需要參與。詢問時給出完整推薦方案、金額與具體操作，避免零碎追問。
- 開發進度只更新 [docs/status.md](docs/status.md)：當前步驟、完成證據、下一步、真正需要使用者決定的事。不新增平行 checklist 或反覆開交接文件。
- 2026-09-14 使用者已批准第六節模型與兩階段預算，可依序執行第二、三步；正式研究仍須先完成開發並凍結。GitHub 發布／部署未授權。

## 五、其他文件怎麼用

**本文決定現在做什麼、先後順序與結案標準；下面只是按需查閱的細節，不是平行待辦。**

- [v1 設計](docs/specs/2026-09-14-v1-design.md)：查業務定義、型別、限制與評估細節。
- [開發驗收案例](docs/acceptance-v1.md)：查手算數值與工程契約；不是保留題。
- [舊 M1–M3 施工交接](docs/plans/2026-09-14-v1-delivery.md)：已執行階段的設計紀錄；未完成階段以本文排序。
- [M4 技術交接](docs/m4-handoff.md)：查 adapter 與既有介面；不要求使用者另外重讀或提供已存在的資料路徑。

如果細節與本文的進度／分工衝突，以本文為準；涉及金額、資料口徑或評估誠信的實質矛盾，寫清具體問題後再處理，不默默改驗收標準。

## 六、模型與費用上限已批准：GPT-5.6 Luna

2026-09-14 使用者明確指定本案使用 **GPT-5.6 Luna**，取代先前 GPT-5.4 mini 提案。三方法固定使用 **`gpt-5.6-luna`**；[OpenAI 官方模型頁](https://developers.openai.com/api/docs/models/gpt-5.6-luna) 目前列出的 model ID 與 snapshot 均為此名稱，不能自行杜撰日期後綴，亦不使用會指向 Sol 的 `gpt-5.6` 別名。Responses API、structured outputs 與 low reasoning 均列為支援；本帳號存取權與實際 API 行為尚未呼叫驗證。

共用參數沿用 `reasoning.effort=low`、`max_output_tokens=4096`、標準服務、SDK `max_retries=0`、每次 timeout 不超過 55 秒且受剩餘任務時間限制。不提供 shell／外部工具，不設定 temperature；三方法的輸出 schema 不同，但共用業務 catalog、可讀 schema、澄清與資源限制。這是專案 API 模型設定，不是更改開發 task 自身模型的指示。

2026-09-14 核對 [OpenAI 官方價格](https://developers.openai.com/api/docs/pricing)：標準短上下文每百萬 token 輸入 **US$0.20**、cached input **US$0.02**、輸出 **US$1.20**；明示 cache write 為 **US$0.25**。本案估算不假設 cache 折扣，也不啟用額外 cache-write 策略。超過 272K 輸入另有長上下文費率，本案不採該範圍；預留費用時仍须依真實輸入上界與實際服務模式判斷。費用未含稅、匯率或區域附加費，不使用 Batch／Fast 或其他付費工具。

| 階段 | 估算呼叫數 | Luna 估計 token 費 | 已批准的獨立硬上限 |
|---|---|---:|---:|
| 公開開發驗證 | 26 題 × 3 方法 × 最多 3 次呼叫 × 最多 3 輪開發 run＝702 calls | **US$5.14** | **US$25** |
| 一次正式研究 | (40 原題 + 12 × 2 相依變體) × 3 方法 × 最多 3 次呼叫＝576 calls | **US$4.21** | **US$20** |

每次仍按 12,000 輸入與 4,096 輸出 token 估算，Luna 單次 US$0.0073152，兩階段合計約 **US$9.35**。輸入長度只是估算假設，非實際量測或現有硬限制；正式結果以 usage 與凍結費率計算。原 US$45 是建議總上限，並非預估花費或必須花完；兩階段不挪用餘額。

**2026-09-14 使用者在本 task 回覆「我相信你的專業，都照你建議的方式進行」，已批准上述 Luna 與開發 US$25／正式研究 US$20 的分項上限。** 已核准範圍不重複詢問；兩階段不挪用、不自行追加。每次呼叫先預留輸入上界＋最大輸出費用，未知 usage 保留預留額，達上限停止。所有開發失敗保留，正式研究仍依第二步完成、版本凍結與保留題規則執行。GitHub／部署／發布仍未授權。

後續依本文施工並只更新 docs/status.md；保留所有現行未提交工作，不從 M1 重做。
